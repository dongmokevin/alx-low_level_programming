#include "main.h"

/**
 * print_alphabet - Prints the Alphabet in Lower case
 *
 * Return: o
 */

void print_alphabet(void)
{
	char ch;

	for (ch = 'a'; ch = 'z'; ch++)
	{
		_putchar(ch);
	}

	_putchar('\n');
}
